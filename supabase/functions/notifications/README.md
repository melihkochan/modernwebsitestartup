# Notifications Dispatcher Edge Function

## Purpose
This function processes system triggers (e.g. new announcements or poll publications) and formats messages to be broadcasted via webhooks, Discord integrations, or local UI pushes.

## Deployment
Deploy via Supabase CLI:
```bash
supabase functions deploy notifications
```
